use v5.38.0;
package Ven;
use Filter::Macro;
use MooseX::Clone;
use assign::0;
#use Perl6::Controls;
use Syntax::Keyword::Match;
use syntax gather => {
  gather => { -as => 'rgather' },
  take   => { -as => 'rtake' },
};
use Quantum::Superpositions;
use Capture::Tiny ':all';
use YAML::XS;
use XXX -with => 'YAML::XS';
